export enum EMAIL_QUEUE_STATUS {
    SENT = "sent",
    FAILED = "failed",
    RETRY = "retry",
}

export enum TEMPLATE_STATUS {
    ACTIVE = "active",
    INACTIVE = "inactive",
    ARCHIVED = "archived",
}